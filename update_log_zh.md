# 更新日志（中文）

> 说明：本文件为 `update_log.md` 的中文同步版。自本次起两份日志会同步更新（同日期、同主题）。

## 2026-03-19
- 建立日志体系与调试目录：新增 `update_log.md`、整理运行日志到 `log/debug/`。
- 持续修复房间同步与刷新恢复：优化 WebSocket 同步顺序、防抖回写、房间恢复重连、房主/成员离房逻辑。
- 初步完成媒体库与导入能力：新增 `/api/media`、媒体导入后入库、房间内媒体选择与同步播放。
- 完成音频模式与双播放器基础：支持视频/音频模式切换与同步。
- 引入管理员面板雏形：用户、媒体、房间的管理接口与页面。

## 2026-03-20
- 后端从单文件重构为 `wt_server/` 多模块结构（配置、存储、鉴权、媒体、路由等）。
- 前端按模块拆分（auth/lobby/chat/api-client/shared），提升可维护性。
- 加强旧媒体 URL 兼容与房间状态修正，减少历史数据导致的灰屏问题。

## 2026-03-21
- 大规模稳定性修复：
  - WS 自动重连 + 心跳检测 + 控制权故障转移；
  - 移动端刷新/密码房重连与缓存恢复；
  - 会话替换与强制下线提示；
  - SQLite 并发与异常分支健壮性增强。
- 完成媒体目录迁移到 `media/work/<work>/`，并加入启动迁移与去重。
- 管理端持续增强：房间删除、媒体重命名、状态可视化、导入表单升级。
- 引入 `cover_url` + `media_type`，支持五种类型与封面规则（上传/抽帧/默认封面）。

## 2026-03-22
- 修复音频导入边界：忽略 `attached_pic` 伪视频流，避免误走视频路径。
- 强化默认封面兜底与失败清理，避免半成品 work 目录残留。
- 统一双端口启动与版本一致性；排查并修复 DDNS 选错 IPv6 的问题。
- 用户端媒体列表改为封面卡片样式，提升浏览体验。

## 2026-03-23
- 用户导入入口迁移到选房页，释放播放页纵向空间；新增上传进度条。
- 批量转码历史高风险视频为移动端友好参数（H.264/yuv420p/L4.1）。
- 管理端改版：左侧标签页 + 右侧大内容区；媒体管理改为封面卡片 + 详情浮窗。
- 管理端新增媒体类型修改能力（五选一）并修复历史 work 无 DB 行时的持久化问题。
- 用户/管理两端新增类型筛选；播放页三列布局定高，避免相互撑高。
- 主题与视觉升级：粉白半透明风格 + 背景图 `day.jpg`；后续扩展到昼夜双主题切换。

## 2026-03-24 00:04
- 修复 DB 缺失回退路径：
  - type 优先读 `.meta.json`，其次 DB，最后猜测；
  - cover 缺失时回退 `/media/default_cover.jpg`。
- 导入与类型修改会同步写 `.meta.json`，并过滤不可播放幽灵条目。

## 2026-03-24 10:53
- 完成 user/admin 双主题（亮/暗）切换：
  - 亮主题：`day.jpg` + 粉白色卡；
  - 暗主题：`night.jpg` + 夜间色卡；
  - 主题本地存储、用户间不互相同步。
- 优化封面态交互：大字浮现、点击空白切登录弹窗、背景渐进模糊、再次点击恢复。
- 修复大厅与页脚版本号布局，确保登录态/封面态都稳定可见。
- 上传提示增强：上传阶段 ETA + 服务器处理阶段动画与预计剩余时间。

## 2026-03-24 13:41
- 管理端导入流程补齐动态提示（与用户端一致）：上传进度、ETA、处理阶段提示。
- 修复“阶段文字来回跳”造成的误判：改为按估算时长线性推进阶段。
- 新增/接入全库 `.meta.json` 回填：旧 work 也统一拥有 JSON 元数据，便于后续扩字段。

## 2026-03-24 13:48
- 新增 `update_log_zh.md`，并约定与 `update_log.md` 同步维护。
