# 更新日志（中文）

> 说明：本文件为 `update_log.md` 的中文同步版。自本次起两份日志会同步更新（同日期、同主题）。

## 2026-03-19
- 建立日志体系与调试目录：新增 `update_log.md`、整理运行日志到 `log/debug/`。
- 持续修复房间同步与刷新恢复：优化 WebSocket 同步顺序、防抖回写、房间恢复重连、房主/成员离房逻辑。
- 初步完成媒体库与导入能力：新增 `/api/media`、媒体导入后入库、房间内媒体选择与同步播放。
- 完成音频模式与双播放器基础：支持视频/音频模式切换与同步。
- 引入管理员面板雏形：用户、媒体、房间的管理接口与页面。

## 2026-03-20
- 后端从单文件重构为 `wt_server/` 多模块结构（配置、存储、鉴权、媒体、路由等）。
- 前端按模块拆分（auth/lobby/chat/api-client/shared），提升可维护性。
- 加强旧媒体 URL 兼容与房间状态修正，减少历史数据导致的灰屏问题。

## 2026-03-21
- 大规模稳定性修复：
  - WS 自动重连 + 心跳检测 + 控制权故障转移；
  - 移动端刷新/密码房重连与缓存恢复；
  - 会话替换与强制下线提示；
  - SQLite 并发与异常分支健壮性增强。
- 完成媒体目录迁移到 `media/work/<work>/`，并加入启动迁移与去重。
- 管理端持续增强：房间删除、媒体重命名、状态可视化、导入表单升级。
- 引入 `cover_url` + `media_type`，支持五种类型与封面规则（上传/抽帧/默认封面）。

## 2026-03-22
- 修复音频导入边界：忽略 `attached_pic` 伪视频流，避免误走视频路径。
- 强化默认封面兜底与失败清理，避免半成品 work 目录残留。
- 统一双端口启动与版本一致性；排查并修复 DDNS 选错 IPv6 的问题。
- 用户端媒体列表改为封面卡片样式，提升浏览体验。

## 2026-03-23
- 用户导入入口迁移到选房页，释放播放页纵向空间；新增上传进度条。
- 批量转码历史高风险视频为移动端友好参数（H.264/yuv420p/L4.1）。
- 管理端改版：左侧标签页 + 右侧大内容区；媒体管理改为封面卡片 + 详情浮窗。
- 管理端新增媒体类型修改能力（五选一）并修复历史 work 无 DB 行时的持久化问题。
- 用户/管理两端新增类型筛选；播放页三列布局定高，避免相互撑高。
- 主题与视觉升级：粉白半透明风格 + 背景图 `day.jpg`；后续扩展到昼夜双主题切换。

## 2026-03-24 00:04
- 修复 DB 缺失回退路径：
  - type 优先读 `.meta.json`，其次 DB，最后猜测；
  - cover 缺失时回退 `/media/default_cover.jpg`。
- 导入与类型修改会同步写 `.meta.json`，并过滤不可播放幽灵条目。

## 2026-03-24 10:53
- 完成 user/admin 双主题（亮/暗）切换：
  - 亮主题：`day.jpg` + 粉白色卡；
  - 暗主题：`night.jpg` + 夜间色卡；
  - 主题本地存储、用户间不互相同步。
- 优化封面态交互：大字浮现、点击空白切登录弹窗、背景渐进模糊、再次点击恢复。
- 修复大厅与页脚版本号布局，确保登录态/封面态都稳定可见。
- 上传提示增强：上传阶段 ETA + 服务器处理阶段动画与预计剩余时间。

## 2026-03-24 13:41
- 管理端导入流程补齐动态提示（与用户端一致）：上传进度、ETA、处理阶段提示。
- 修复“阶段文字来回跳”造成的误判：改为按估算时长线性推进阶段。
- 新增/接入全库 `.meta.json` 回填：旧 work 也统一拥有 JSON 元数据，便于后续扩字段。

## 2026-03-24 13:48
- 新增 `update_log_zh.md`，并约定与 `update_log.md` 同步维护。

## 2026-03-24 18:43
- 上传上限从 1GB 提升到 1.5GB（后端导入限制与用户/管理员前端错误提示同步更新）。
- 新增防重复上传保护（按文件名/大小/lastModified 生成指纹），减少误触重复提交导致的 `_2` 后缀 work。
- 优化用户/管理员导入处理阶段文案：按估算时间线性推进阶段，不再快速循环跳动造成“卡住”错觉。
- 修复管理员 guest 封面态底部版本号定位：改为底部居中固定并提升层级，避免移动端与中间按钮重叠。

## 2026-03-24 19:24
- 修复管理员页底部版本号定位“看起来未生效”的根因：`admin.html` 仍引用旧版样式参数（`admin.css?v=20260324c`），导致最新 footer 样式未实际加载。
- 已将管理员页样式引用统一到当前版本（`admin.css?v=20260324j`），与脚本/构建号保持一致。

## 2026-03-26 09:10
- 修复房间同步中“影片错位”问题：在部分远端同步包下，成员端可能在进度仍同步时错误切到其他影片；现已收紧前端模式切换逻辑，避免远端状态处理中的冗余换源。
- 新增房间同步调试日志体系，便于定位多人房间状态漂移：
  - 后端将结构化 WS 同步事件写入 `log/debug/sync.log`（带轮转），
  - 前端在浏览器控制台输出 `[WT_SYNC]` 前缀的结构化同步日志。
- 新增同步日志配置项（`wt_server/config.py`）：
  - `LOG_DEBUG_DIR`、`SYNC_LOG_FILE`，
  - 环境变量 `WATCH_SYNC_DEBUG`（默认开启）。
- 启动初始化时确保调试日志目录自动创建（`storage.init_db`）。
- 更新用户端前端版本号（`app.js?v=20260326a`，页脚构建号 `20260326a`）。

## 2026-03-26 09:34
- 根据 `sync.log` 进一步定位并修复“仍偶发不同源”的根因：在换源/事件竞态下，仍会有部分同步包携带空 `videoUrl`，导致其他成员只同步了进度/播放状态但媒体源未切换。
- 加固客户端同步上报（`static/app.js`）：`sendSync()` 在 `player.currentSrc/src` 暂时为空时，会回退到当前激活媒体项计算路径，避免发送空媒体 URL 的同步包。
- 加固远端状态应用：当本地已有激活媒体时，收到空 `videoUrl` 的远端状态将直接忽略，避免误清空或错误重绑媒体源。
- 新增对应调试日志事件：空媒体远端状态忽略、以及无媒体路径导致的同步发送跳过。
- 更新用户端前端版本号（`app.js?v=20260326b`，页脚构建号 `20260326b`）。

## 2026-03-26 09:47
- 修复上一轮加固后引入的回归问题：当播放器字段里没有稳定媒体 URL 时，客户端可能持续发不出有效媒体路径，表现为“进度和换片都不同步”。
- 在 `static/app.js` 新增持久兜底字段 `lastSyncedMediaPath`：
  - 本地选片或收到远端有效媒体状态时更新；
  - `sendSync()` 在 `currentSrc/src/activeMediaUrl` 暂时不可用时，以此作为最后兜底，避免空 URL 同步包。
- 优化 `setPlayMode()` 的激活媒体匹配：改为同时匹配 `videoUrl` 与 `audioUrl`，避免仅按当前模式 URL 匹配导致的切模式丢源。
- 切模式成功换源时同步刷新媒体路径缓存，确保后续同步包仍带有效媒体路径。
- 更新用户端前端版本号（`app.js?v=20260326c`，页脚构建号 `20260326c`）。

## 2026-03-26 10:03
- 修复新导入/中文命名媒体的重大同步兼容问题（如“辉夜姬”）：服务端媒体 URL 解析在做文件存在性校验前未对路径分段进行 URL 解码，导致合法的编码路径被误判为不存在并归一化为 `""`。
- 已在 `wt_server/media.py` 中更新媒体 URL 分割逻辑：对路径分段使用 `urllib.parse.unquote` 后再参与校验。
- 现象与根因一致：老的 ASCII 命名作品（如 `mare260323`）基本正常，而新导入中文名作品更容易出现同步包里 `videoUrl` 为空。
- 更新用户端前端版本号（`app.js?v=20260326d`，页脚构建号 `20260326d`），便于本次后端修复后的统一刷新验证。

## 2026-03-26 10:12
- 调整播放页左侧 `Media Library` 卡片尺寸，缩小为更合适的展示密度。
- 具体样式优化：减小卡片间距、限制卡片最大宽度为 `190px`、封面比例调整为 `16:10`，并收紧名称行字号与内边距。
- 更新用户端静态资源版本（`styles.css?v=20260326e`、`app.js?v=20260326e`、页脚构建号 `20260326e`）。

## 2026-03-26 10:16
- 按移动端适配要求调整播放页左侧 `Media Library`：卡片改为跟随容器宽度并保持单列（一行一个），避免在手机上显示过小。
- 移除固定小卡片宽度上限，媒体卡片改为填充当前列可用宽度。
- 更新用户端静态资源版本（`styles.css?v=20260326f`、`app.js?v=20260326f`、页脚构建号 `20260326f`）。

## 2026-03-26 10:27
- 修复成员加入/退出房间时，播放进度偶发被重置到 `0:00` 的问题。
- 根因：在成员在线状态变化叠加远端状态应用/播放器换源时，部分“非用户手动触发”的媒体事件仍可能进入本地同步链路，发出带强制接管的低时间戳同步包，从而把房间进度拉回。
- 前端在 `onLocalPlaybackAction` 新增防护：忽略非用户触发事件（`event.isTrusted === false`），避免成员变更时误触发本地同步重置。
- 更新用户端静态资源版本（`styles.css?v=20260326g`、`app.js?v=20260326g`、页脚构建号 `20260326g`）。

## 2026-03-26 10:38
- 进一步加固成员离开/刷新场景下的同步稳定性：
  - 前端本地播放事件同步新增“用户手势新鲜度”门控（约 `1.8s`），阻断延迟触发或非意图事件导致的误接管同步包，避免离线/重连时仍有概率重置进度。
  - 后端新增成员在线变更时的状态再广播（`system-presence-sync`）：成员加入时、非控制者离开时都会把当前房间权威状态再推一遍，确保所有端媒体源与进度快速收敛。
- 同时改进“成员加入时偶发拿不到视频源”的问题：通过 presence 同步广播补齐状态到达。
- 更新用户端静态资源版本（`styles.css?v=20260326h`、`app.js?v=20260326h`、页脚构建号 `20260326h`）。

## 2026-03-26 10:52
- 回退前端“手势新鲜度门控”策略：该策略在部分正常操作链路下会误伤有效同步，导致“我这边改了，对面没反应”的概率上升。
- 增加后端对空媒体同步包的硬化处理：
  - 当客户端上报 `videoUrl` 为空，但房间已有有效媒体源时，服务端会自动回填当前房间媒体源再入库/广播；
  - 防止刷新/离线等边缘过程把媒体源覆盖成空值，从而触发进度重置。
- 该方案直接针对“成员退出/刷新后仍会重置进度”的剩余问题，同时保持正常同步响应速度。
- 更新用户端静态资源版本（`styles.css?v=20260326i`、`app.js?v=20260326i`、页脚构建号 `20260326i`）。

## 2026-03-26 11:08
- 基于根因修复刷新/离开导致的进度重置：
  - 确认问题来自“远端状态应用后”仍会晚到一批受信任媒体事件，这些事件会误触发本地接管同步；
  - 在前端加入短暂本地同步阻断窗口（`blockLocalSyncUntil`），远端状态应用后的过渡阶段不允许本地事件再次发起同步。
- 保留手动操作灵敏度：从媒体库点选时会清除阻断窗口，不影响正常切片与控制。
- 该方案用于抑制刷新/离开时误发 `forceTakeover + 低时间戳` 包，不通过粗暴的服务端时间钳制来掩盖问题。
- 更新用户端静态资源版本（`styles.css?v=20260326j`、`app.js?v=20260326j`、页脚构建号 `20260326j`）。

## 2026-03-26 11:20
- 基于最新同步日志继续定位并修复剩余重置触发点（非拍脑袋加保护）：
  - 根因确认：在换源场景下，客户端有时在媒体元数据尚未就绪前就应用 `currentTime/isPlaying`，后续浏览器补发播放事件再次进入本地同步，产生 `0s` 接管包。
- 重构 `static/app.js` 的 `applyRemoteState` 执行时序：
  - 换源后等待 `loadedmetadata/canplay`（含超时兜底）再执行 seek/play/pause；
  - 统一收敛到同一阶段应用播放状态，并收紧漂移阈值，降低临界条件下反复 seek/播放竞态。
- 该修复直接针对刷新/离开诱发的进度条回零问题，仍未引入服务端时间钳制策略。
- 更新用户端静态资源版本（`styles.css?v=20260326k`、`app.js?v=20260326k`、页脚构建号 `20260326k`）。

## 2026-03-26 11:33
- 根据最新日志继续定位：仍存在“编码 URL 与解码 URL 不一致”导致的误判换源问题，远端状态应用阶段会把同一媒体当成不同源，触发重复换源并放大重连/离房时的重置现象。
- 在 `static/app.js` 增加媒体路径规范化比较（`decodeURIComponent` 后的 canonical path）：
  - 远端换源判定改为 canonical path 比较，
  - 媒体库匹配也改为 canonical path，比对不再依赖原始 URL 字符串。
- 修正 UI：收窄播放页左侧媒体列宽，恢复桌面端更紧凑的卡片观感；移动端仍保持单列满宽，不会变小。
- 更新用户端静态资源版本（`styles.css?v=20260326l`、`app.js?v=20260326l`、页脚构建号 `20260326l`）。

## 2026-03-26 11:45
- 回退后端 websocket 断开阶段的 presence 状态再广播（`routes.py` finally）：已确认该广播会干扰正在播放房间的权威状态，成员刷新/离房频繁时可能诱发进度跳变或回零感知。
- 断开时仅保留成员列表广播，播放状态继续由正常同步流维护。
- UI 按你的要求纠正：桌面端媒体库卡片恢复为更紧凑的视觉尺寸；移动端仍保持单列满宽自适应。
- 更新用户端静态资源版本（`styles.css?v=20260326m`、`app.js?v=20260326m`、页脚构建号 `20260326m`）。

## 2026-03-28 18:56
- 新增 **仅针对纯音频 work** 的多音频顺序播放支持，视频 work 逻辑保持不变。
- 后端现在会为纯音频作品目录扫描多个音频文件，在媒体库接口中返回 `audioTracks`，并在房间状态 / WebSocket 状态里根据当前选中音频 URL 推导 `trackIndex`。
- 用户端房间页面在音频模式下会为多音频纯音频作品显示紧凑的 track 选择器；切换 track 时会从该 track 的 `0s` 开始，保留当前播放/暂停意图，并同步给房间成员。
- 管理员媒体详情页现在会按顺序列出纯音频作品的所有音频 track，而不再只隐含一个单独音频文件。
- 由于刷新/离房重置根因已经定位并修复，这次同步也顺带放宽了部分先前过严的前端时间窗口保护，以改善暂停/播放/拖动的跟手性。
- 补充修正：
  - 后端 `trackIndex` 推导现在会统一处理编码/解码后的 URL，避免文件名或作品名包含空格、中文等字符时索引回退错误；
  - 纯音频作品不再允许切换到无效的视频模式；
  - 在音频模式下再次点击同一个纯音频作品卡片时，会保留当前选中的 track，而不是跳回第 1 首；
  - 从音频模式离开房间时，房间 UI 会完整复位到视频模式状态。
- 更新静态资源版本（`styles.css?v=20260326p`、`app.js?v=20260326p`、`admin.js?v=20260326p`、构建号 `20260326p`）。

## 2026-03-28 19:42
- 将管理员导入系统升级为支持 **现有纯音频专辑追加导入**。
- 后端导入逻辑（`wt_server/media.py`）新增 `append_audio` 模式：
  - 校验目标作品存在且必须为纯音频作品；
  - 追加模式下拒绝封面上传与包含视频流的文件；
  - 追加音频会转码为 AAC `.m4a` 保存到原作品目录，并通过 `.meta.json` 的 `audioTrackOrder` 保持稳定排序。
- 管理员导入界面（`static/admin.html`、`static/admin.js`）新增：
  - `Create new work` / `Append to audio album` 模式切换；
  - 仅展示现有纯音频作品的目标下拉框；
  - 追加模式下的字段禁用、校验与专用成功/失败提示。
- 用户端上传流程刻意保持为仅创建新作品，不引入追加模式，确保现有兼容性。
- 已用临时生成的纯音频专辑做追加导入验证：追加成功，`audioTracks` 正确扩展，默认 `audioUrl` 仍保持为第一首。
- 更新静态资源版本（`styles.css?v=20260328a`、`app.js?v=20260328a`、`admin.js?v=20260328a`、构建号 `20260328a`）。

## 2026-03-28 20:06
- 修复“删空后的纯音频专辑无法再作为追加目标”的问题。
- 后端新增管理员追加目标列表接口：即使某个 audio-only work 当前 `0` 条 track，只要作品目录和 `.meta.json` 仍存在，就会继续出现在追加目标列表中。
- 管理员追加下拉框改为使用专用追加目标接口，而不是仅依赖当前可见媒体库项，因此像 `RJ01507563` 这种被删空的专辑也能继续被选中追加。
- 已用临时空专辑壳验证：追加目标列表会返回 `trackCount: 0`，并可用于后续追加导入。
- 更新静态资源版本（`styles.css?v=20260328b`、`app.js?v=20260328b`、`admin.js?v=20260328b`、构建号 `20260328b`）。

## 2026-03-28 20:18
- 跟进修正管理员追加目标下拉框交互：当处于追加模式且存在可选目标时，会自动选中第一个有效目标，而不再停留在空白占位状态。
- 这能避免目标列表刷新后表单看起来“一个也选不了”的假死状态。
- 更新静态资源版本（`styles.css?v=20260328c`、`app.js?v=20260328c`、`admin.js?v=20260328c`、构建号 `20260328c`）。

## 2026-03-28 20:25
- 进一步加固管理员追加目标加载：切换到 `Append to audio album` 时会主动重新请求追加目标列表，而不再只依赖仪表盘初次加载时的一次请求。
- 这用于修复“后端实际上已有可追加专辑（包括 `RJ01507563` 这种空专辑），但下拉框仍显示为空”的情况。
- 更新静态资源版本（`styles.css?v=20260328d`、`app.js?v=20260328d`、`admin.js?v=20260328d`、构建号 `20260328d`）。

## 2026-03-26 12:02
- 在已定位并修复刷新/离房重置根因后，放宽此前过严的前端同步保护。
- 移除播放同步链路中的 `blockLocalSyncUntil` 阻断门控，并将本地覆盖窗口从 `1200ms` 下调为 `450ms`，提升拖动/暂停/播放同步跟手性。
- 将同步发送防抖从 `40/120ms`（接管/普通）收紧为 `20/60ms`，提高多端反应速度。
- 更新用户端静态资源版本（`styles.css?v=20260326n`、`app.js?v=20260326n`、页脚构建号 `20260326n`）。
